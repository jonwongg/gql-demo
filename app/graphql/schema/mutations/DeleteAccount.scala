package graphql.schema.mutations

import graphql.QueryContext
import sangria.schema.Context

import scala.concurrent.Future
import sangria.schema.{Argument, StringType}

object DeleteAccount {

//  def mutate(ctx: Context[QueryContext, Unit]): Future[Int] = {
//    ctx.ctx.services.accountService.delete(
//      accountId = ctx.arg(Argument("id", StringType))
//    )
//  }
}
